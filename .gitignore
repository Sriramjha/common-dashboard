# ── Secrets — NEVER commit these ──────────────────────────────
# AWS root/IAM access key exports (never commit)
*accessKeys*.csv
Sri_User_accessKeys.csv
deploy/.keys/

.env
*.env
.env.*
!.env.example

# Coralogix snapshot (contains account metadata)
dashboard_snapshot.json

# Live dashboard data — generated by refresh.py
# Contains account metadata. NO credentials inside, but gitignored anyway.
data.json
data.*.json

# Multi-account manifest (copy accounts/manifest.example.json → accounts/manifest.json)
accounts/manifest.json
_debug_data_usage_body.txt

# API tool output cache (may contain log data)
agent-tools/

# macOS
.DS_Store
**/.DS_Store

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Node / npm
node_modules/
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Handoff zips (safe to share; don’t commit — large / churn)
coralogix-dashboard-SHARE-READY.zip
coralogix-dashboard-share-*.zip
